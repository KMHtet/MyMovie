const ClassifyEnum = {
  
};

const EventEnum = {
};

const StorageEnum = {
};

const StatusEnum = {
};

const ScreenEnum = {
};

export {EventEnum, ClassifyEnum, StorageEnum, StatusEnum, ScreenEnum};
